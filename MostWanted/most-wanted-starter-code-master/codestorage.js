function displayFamily(famInfo, data){
  // let famInfoAlert = 
  // " Direct Family: " + 
  data.filter (function(el) {
    if(el.id === parents[0].famInfo){
      return true;
    }
    else {
      return false;
    }
  }
    )
  if {
    id === famInfo.parents
    return true;
  } 

  else {
    return false;
  }

  alert(famInfoAlert);
}
// take two
function displayFamily(data, personPass){
  // let famInfoAlert = 
  // " Direct Family: " + 
  let nameSpouse;
  let nameParents;
  let nameChildren = [];

  if (personPass.currentSpouse === null) {
      let spouse = data.filter(function(person){
    if (person.id === personPass.currentSpouse){
        return true;
    }else{
      return false;
    }
  });

  }

  // loop throught the dataset

  for (let i= 0; i <= data.length; i++) {
    if (data[i].parents)
    
  }

  if  {
    
  }
